// function that receives a string and changes it into capital letters.
function capitalLetter(){
    const promptSync=require("prompt-sync");
    let prompt=promptSync();
    let string="shout up";
    let capital=string.toUpperCase()
    console.log(capital)}
    capitalLetter();